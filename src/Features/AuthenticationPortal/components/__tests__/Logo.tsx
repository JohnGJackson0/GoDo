import React from "react";
import { render } from "../../../../../test-utils";
import Logo from "../Logo";

describe("Features/AuthenticationPortal/Components/logo", () => {
  it("renders", () => {
    render(<Logo />);
  });
});
